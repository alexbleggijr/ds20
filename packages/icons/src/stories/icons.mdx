import {
  IconGallery,
  IconItem,
  Meta,
  Subtitle,
  Title,
} from '@storybook/blocks';
import {
  TP_ICONS_A,
  TP_ICONS_B,
  TP_ICONS_C,
  TP_ICONS_D,
  TP_ICONS_E,
  TP_ICONS_F,
  TP_ICONS_G,
  TP_ICONS_H,
  TP_ICONS_I,
  TP_ICONS_K,
  TP_ICONS_L,
  TP_ICONS_M,
  TP_ICONS_N,
  TP_ICONS_O,
  TP_ICONS_P,
  TP_ICONS_Q,
  TP_ICONS_R,
  TP_ICONS_S,
  TP_ICONS_T,
  TP_ICONS_U,
  TP_ICONS_V,
  TP_ICONS_W,
  TP_ICONS_X,
  TP_ICONS_Z,
} from './utilities/constants/icon.constant';

<Meta title="Ionic" />

<Title>Icons</Title>

## Usage

```html
<ion-icon src="tp-activity.svg"></ion-icon>
```

<Title>A</Title>
<IconGallery>
  {TP_ICONS_A.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>B</Title>
<IconGallery>
  {TP_ICONS_B.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>C</Title>
<IconGallery>
  {TP_ICONS_C.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>D</Title>
<IconGallery>
  {TP_ICONS_D.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>E</Title>
<IconGallery>
  {TP_ICONS_E.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>F</Title>
<IconGallery>
  {TP_ICONS_F.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>G</Title>
<IconGallery>
  {TP_ICONS_G.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>H</Title>
<IconGallery>
  {TP_ICONS_H.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>I</Title>
<IconGallery>
  {TP_ICONS_I.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>K</Title>
<IconGallery>
  {TP_ICONS_K.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>L</Title>
<IconGallery>
  {TP_ICONS_L.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>M</Title>
<IconGallery>
  {TP_ICONS_M.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>N</Title>
<IconGallery>
  {TP_ICONS_N.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>O</Title>
<IconGallery>
  {TP_ICONS_O.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>P</Title>
<IconGallery>
  {TP_ICONS_P.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>Q</Title>
<IconGallery>
  {TP_ICONS_Q.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>R</Title>
<IconGallery>
  {TP_ICONS_R.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>S</Title>
<IconGallery>
  {TP_ICONS_S.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>T</Title>
<IconGallery>
  {TP_ICONS_T.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>U</Title>
<IconGallery>
  {TP_ICONS_U.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>V</Title>
<IconGallery>
  {TP_ICONS_V.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>W</Title>
<IconGallery>
  {TP_ICONS_W.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>X</Title>
<IconGallery>
  {TP_ICONS_X.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>

<Title>Z</Title>
<IconGallery>
  {TP_ICONS_Z.map((icon) => (
    <IconItem name={`${icon}.svg`}>
      <ion-icon
        src={`./src/${icon}.svg`}
        style={{
          '--stroke': '#000',
          '--fill': '#fff',
        }}
      ></ion-icon>
    </IconItem>
  ))}
</IconGallery>
